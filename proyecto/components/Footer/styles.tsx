import React from 'react'
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    containerFooter: {
        backgroundColor: '#91734C',
        borderRadius: 60,
        flexDirection: 'row',
        justifyContent: 'space-around',
        opacity: 10,
        paddingVertical: 10,
        marginVertical: 40,
        width: '100%',
    }
})

export default styles
